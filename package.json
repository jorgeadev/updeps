{
  "name": "updeps-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "🚀 The ultimate interactive npm dependency updater — monorepo",
  "license": "CC-BY-NC-4.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "pnpm --filter updeps test",
    "lint": "pnpm --filter updeps lint",
    "dev:web": "pnpm --filter updeps-web dev",
    "build:web": "pnpm --filter updeps-web build"
  }
}
